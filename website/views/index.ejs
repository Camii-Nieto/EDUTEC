<!DOCTYPE html>
<html>
  <head>
    <%- include('./partials/meta')%>
  </head>
  <body>
    <header>
      <div class="container">
          <div class="titulo-bienvenidos">
              <h1 class="bienvenidos mt-4">Bienvenidos a EDUTEC</h1>
               <h2 class="sub-bienvenidos mb-4">¿Que estas buscando?</h2> 
          </div>
  

          <div class="seleccion-serv d-flex justify-content-center row">
            <% selectionServ.forEach(selectionServ => { %>
              <div class="d-flex flex-column  m-5 col-xs-10 col-sm-10 col-md-5 col-lg-5">
                  <img src="<%= selectionServ.img %>" alt="<%= selectionServ.imgAlt %>" class="img-bienvenidos">
                  <a href="<%= selectionServ.page %>" class="btn-bienvenidos"><%= selectionServ.btnDescription %></a>
              </div>
            <% }) %>
          </div>
      </div>
  </header>
  </body>
</html>
